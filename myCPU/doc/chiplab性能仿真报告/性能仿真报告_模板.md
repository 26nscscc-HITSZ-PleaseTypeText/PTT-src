# myCPUVx.x：〈本轮主题〉— 性能仿真报告

> 日期：YYYY-MM-DD  
> **基线报告/CPU**：〈上一份报告文件名或简述；写明基线 Linux IPC〉  
> 本轮改动：〈一句话〉  
> 负载：`func_lab19` digftest；Linux 软门 / digftest（分列）  
> seed：`5570815`（`BUS_DELAY_RANDOM_SEED`）  
> 原始 PERF 日志：〈如 `/tmp/xxx_lab19.log`、`/tmp/xxx_linux_soft.log`〉  
> PERF 口径：仿真结束 `myCPU PERF (sim)`（`mycpu_top.v` 中 `` `ifdef SYNTHESIS ``；仿真经 `-DSIMU` 在 `mycpu.h` 定义 `SYNTHESIS`，Vivado 综合勿定义）

---

## 0. 结论摘要

| 门控 | 结果 |
|------|------|
| **lab19 digftest** | PASS / FAIL（`END by Syscall` / …） |
| **Linux 软门** | PASS / FAIL（`/ #`）；IPC = … |
| **Linux digftest** | PASS / FAIL / 未跑；IPC = …（勿与软门混比） |

**一句话：** 〈功能是否过 + 相对基线 IPC 升降 + 主瓶颈〉

---

## 1. 本轮改动

| 项 | 留树 / 回退 | 说明 |
|----|-------------|------|
| … | ✅ / ❌ | … |

合入类报告另附「探索未留树」与备份路径（`*.bak`）。

---

## 2. 功能结果

| 负载 | 判据 | 证据（日志关键字 / 路径） |
|------|------|---------------------------|
| lab19 | `END by Syscall` / HIT GOOD | … |
| Linux 软门 | UART `/ #`，干净停仿打 PERF | … |
| Linux digftest | 无 mismatch + 到 `/ #`（或明确写未到） | … |

---

## 3. 性能对比（相对基线）

主对比表用 **Linux 软门**；同 seed、同停仿条件才可比 IPC。

| 指标 | 基线 | 本轮 | Δ |
|------|------|------|---|
| Commit IPC | | | |
| Commit dual-issue | | | |
| Dispatch dual / dual_ALU | | | |
| BPU all / cond | | | |
| I$ hit | | | |
| D$ hit / MSHR busy | | | |
| ROB full / RS_MEM stall | | | |
| FTB/TAGE overflow | | | |

---

## 4. 解读（必写）

- **优化：** 本轮做了什么、为何可能提速  
- **提升：** 相对基线哪些指标变好  
- **不足：** 未达目标 / 回归 / 仍高的 stall  
- **下一步：** 可验证的下一刀（避免空泛）

---

## 5. 怎么测（复现）

环境：`CHIPLAB_HOME`；seed **`5570815`**。改 RTL 后必须 `make verilator && make testbench`（不能只 `make run`）。换 case 前 `make clean_all` 再 `configure.sh`。

| 负载 | 配置（典型） | 成功标志 | 性能用途 |
|------|----------------|----------|----------|
| **lab19 硬门** | `./configure.sh --run func/func_lab19`（digftest 开） | `END by Syscall` / HIT GOOD | 功能正确；IPC 仅作短测参考 |
| **Linux 软门（性能主对比）** | `./configure.sh --run linux --disable-simu-trace --disable-trace-comp --output-uart-info` | UART `/ #`，干净停仿打 PERF | **IPC/stall 与基线对比用这个** |
| **Linux digftest** | 同上但**去掉** `--disable-trace-comp` | 无 mismatch + 到 `/ #`（或报告明确写未到） | 正确性；IPC 勿与软门混比 |

```bash
export CHIPLAB_HOME=...
cd $CHIPLAB_HOME/sims/verilator/run_prog
make clean_all
./configure.sh --run <case> ...
make soft_compile
make verilator && make testbench
make simulation_run_prog
```

摘数：日志中 `==================== myCPU PERF (sim) ====================` 至结束；完整 log 路径写入头注。

### 注意事项

- 同 seed、同停仿条件（`/ #` / Syscall）才可比 IPC  
- 软门 vs digftest **分列**，主性能表用软门  
- PERF 仅仿真；上板综合不依赖层次探针 / `final` dump  
- 合入类报告另加「探索未留树」与备份路径  

---

## 6. 必测性能指标清单

探针来源：仿真结束 `myCPU PERF (sim)`。报告至少覆盖下表。

### 吞吐 / 提交

| 指标 | PERF 行关键字 | 用途 |
|------|----------------|------|
| Commit IPC | `Commit IPC` | 主对比；相对基线算 Δ% |
| 双提交率 | `Commit dual-issue` / `vs all cycles` | 窗口是否吃满 |
| Dispatch dual / dual_ALU | `Dispatch dual` | 前端能否供双发 |
| Rename/dispatch stall | `turnover_refill` / `dispatch_stall` / `rob_stall` | rename 阻塞 vs ROB 满 |

### 预测 / 取指

| 指标 | PERF 行关键字 | 用途 |
|------|----------------|------|
| BPU all / cond 准确率 | `BPU all-branch` / `BPU cond-branch` | 误预测成本 |
| FTB P1 hit | `FTB (P1 response)` | 前端命中 |
| I$ hit / access | `ICache (cached)` | 取指瓶颈 |
| FTB/TAGE update overflow | `FTB update queue` / `TAGE update queue` | 训练丢失 |

### 访存

| 指标 | PERF 行关键字 | 用途 |
|------|----------------|------|
| D$ hit（总/ld/st） | `DCache (cached)` 及 load/store 子行 | 缓存效果 |
| D$ MSHR busy / pend / MWAIT | `D$ MSHR busy` / `pend` / `MWAIT` | miss 占用 |
| LSU store-order / DC wait | `LSU store-order` / `LSU DC wait` | STQ/序与等数据 |
| RS_MEM src / LSU / full stall | `RS_MEM *` | 访存发射堵点 |
| SB full | `SB  full cycles` | 提交后写缓冲 |

### 队列占用（avg / max / cap / ≥75% / full）

| 队列 | PERF 行 | 缩减建议依据 |
|------|---------|--------------|
| ROB pairs / valid | `ROB pairs` / `ROB valid` | full% 高则不宜砍 |
| RS ALU0/1、MEM、MDU | `RS ALU*` / `RS MEM` / `RS MDU` | 顶满则伤 IPC |
| SB、IB、FTQ、RAS、RAT | 对应行 | 同左 |
| FTB/TAGE update Q max | 上表 overflow 行 | overflow>0 考虑加深 |

---

## 附录：PERF 摘录

### lab19

```
〈粘贴关键行或完整 PERF 块〉
```

### Linux（软门；若另有 digftest 请分列）

```
〈粘贴关键行或完整 PERF 块〉
```
